node_modules/
dist/
.env
*.log
.upstream/
phase0/.env.spike
phase0/bridge/node_modules/
phase0/bridge/state.json
demo-acceptance/
data/
demo-live/
# Live SQLite runtime state (WAL/SHM are per-process; the .db carries the
# author's catch-up cursor) — never a committed artifact.
demo-fifa/*.db
demo-fifa/*.db-shm
demo-fifa/*.db-wal
repos/.pack-cache/
dkg-mirror/*
!dkg-mirror/sync.sh
wot-knowledge-ui-clone/
wot-autocapture-state.json
*.log
