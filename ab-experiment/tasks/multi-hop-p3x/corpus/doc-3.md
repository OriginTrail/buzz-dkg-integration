# Integration memo 3

event-router pins the wire format of ledger-sync; therefore event-router must be upgraded strictly before ledger-sync.

Unrelated context: doc-store and auth-gateway share a dashboard. cvetka owns the rollout ticket.
