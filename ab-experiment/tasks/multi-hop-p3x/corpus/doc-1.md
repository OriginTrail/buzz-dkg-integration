# Integration memo 1

auth-gateway issues the tokens verified by quote-engine; therefore auth-gateway must be upgraded strictly before quote-engine.

Unrelated context: rate-limiter and batch-indexer share a dashboard. ana owns the rollout ticket.
