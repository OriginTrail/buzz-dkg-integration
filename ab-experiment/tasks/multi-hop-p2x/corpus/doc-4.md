# Integration memo 4

ledger-sync issues the tokens verified by event-router; therefore ledger-sync must be upgraded strictly before event-router.

Unrelated context: schema-registry and rate-limiter share a dashboard. bojan owns the rollout ticket.
