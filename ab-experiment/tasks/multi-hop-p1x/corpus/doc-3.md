# Integration memo 3

event-router consumes the schema published by doc-store; therefore event-router must be upgraded strictly before doc-store.

Unrelated context: batch-indexer and ledger-sync share a dashboard. franc owns the rollout ticket.
