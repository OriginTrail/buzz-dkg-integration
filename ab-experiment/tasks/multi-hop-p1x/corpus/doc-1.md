# Integration memo 1

batch-indexer consumes the schema published by schema-registry; therefore batch-indexer must be upgraded strictly before schema-registry.

Unrelated context: ledger-sync and auth-gateway share a dashboard. franc owns the rollout ticket.
