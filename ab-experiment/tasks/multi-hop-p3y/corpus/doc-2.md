# Integration memo 2

schema-registry pins the wire format of ledger-sync; therefore schema-registry must be upgraded strictly before ledger-sync.

Unrelated context: event-router and auth-gateway share a dashboard. cvetka owns the rollout ticket.
