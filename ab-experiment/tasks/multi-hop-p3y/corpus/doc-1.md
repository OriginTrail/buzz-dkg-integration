# Integration memo 1

ledger-sync pins the wire format of event-router; therefore ledger-sync must be upgraded strictly before event-router.

Unrelated context: batch-indexer and rate-limiter share a dashboard. bojan owns the rollout ticket.
